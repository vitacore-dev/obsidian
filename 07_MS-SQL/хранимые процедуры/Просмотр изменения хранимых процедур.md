для просмотра хронологии изменения хранимой процедуры необходимо выполнить запрос:

```
select * from COMMAND_CHANGE
where CommandName = 'sp_LoadSchet173Act'
order by OperationDate desc
```

где sp_LoadSchet173Act пример наименования хранимой процедуры