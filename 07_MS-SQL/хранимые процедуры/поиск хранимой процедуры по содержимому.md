пример запроса поиска хранимой процедуры по содержимому
```
SELECT definition
FROM sys.sql_modules t
inner join sys.procedures t1
on t.object_id=t1.object_id
WHERE definition LIKE '%ComplicationDiseases%'
```