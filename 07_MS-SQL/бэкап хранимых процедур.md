# Как сделать бэкап хранимых процедур 

1. выполняем в нужной БД запрос
```
SELECT definition
FROM sys.sql_modules t
inner join sys.procedures t1
on t.object_id=t1.object_id
```
2. Результат сохраняем себе в текстовый файл