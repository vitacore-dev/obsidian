# Вывод обычного exception
```
begin()   
    $throw new exception("Зафиксирована попытка удаления роли подписи, обратитесь в МИАЦ!");
end; 
```
# Вывод окна

```
begin(str, mb, p)   
    @str:= "текст ошибки";
    @mb := new MessageBoxInfo(@str, "Предупреждение", 0, 8);
    @p := new Process("GenerateMessageBoxOnClient", @mb);
    @p -> Run();
        end; 
```