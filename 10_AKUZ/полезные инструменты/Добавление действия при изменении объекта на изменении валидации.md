![[Pasted image 20260422160408.png]]


```
begin(us,VC,rso)
  try
    begin()
        declare Rab.VC(UserID:string,ValidationConditionID:string,ChangeDateTime:string);
        @us:=param(CURRENT_USER);    
        @VC := new Rab.VC();
        @VC -> {UserID}:= @us;
        @VC -> {ValidationConditionID} := {ValidationConditionID};
        @VC -> {ChangeDateTime} := param(CURRENT_DATE_TIME);
        @VC := Replace(Str(@VC,"Json"),"\"flags_\":\"1\",","");
        //print(@VC);
        @rso:=new AKUZ.RABBIT_SEND_OBJECT(new Apartment());
        @rso -> {QueueName}:="";
        @rso -> {State}:=10;
        @rso -> {UserID} := @us;
        @rso -> {Content}:=@VC;
        @rso -> {Type}:="VALIDATION_CONDITION";
        @rso -> {CreateDateTime}:= param(CURRENT_DATE_TIME);
        @rso -> Save();
    end
  catch(ex)
    begin()
    
    end;
end;
```


Пример для просмотра:
![[Pasted image 20260422160443.png]]
